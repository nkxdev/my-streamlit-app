ANTHROPIC_API_KEY=your_api_key_here

# Set USE_MOCK_API=true to force the local mock API even when a real key is set.
# When ANTHROPIC_API_KEY is missing or set to "your_api_key_here" the mock is
# used automatically — you do not need to change this setting.
USE_MOCK_API=false
